AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_DEFAULT_REGION=""
AWS_SESSION_TOKEN=""

# --- Opcional: bucket y rutas (para scripts de carga) ---
# AWS_S3_BUCKET=mi-bucket-analitica
# AWS_S3_RAW_PREFIX=raw/empleados

# --- MongoDB Atlas (solo si cargas datos desde local con Python) ---
# MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/

# --- RDS (solo si te conectas desde local; Glue usa conexión JDBC en consola) ---
# RDS_HOST=
# RDS_USER=
# RDS_PASSWORD=
# RDS_DATABASE=
